#include "voicewidget.h"

VoiceWidget::VoiceWidget(QWidget *parent)
    : QWidget(parent)
{
    setupUi(this);
}

CollapsedVoiceWidget::CollapsedVoiceWidget(QWidget *parent)
    : QWidget(parent)
{
    setupUi(this);
}

// vim: sw=4 sts=4 et tw=100
